version: 1
project_name: sp-caddy-manager

builds:
  - main: .
    binary: sp-caddy-manager
    # Remove static linking flags to create dynamically linked binaries
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=1
    flags:
      - -buildmode=default
    # Note: Linux amd64 and arm64 builds enabled with QEMU support for cross-compilation
    # Explicitly disable darwin and windows builds - only Linux supported - will re-enable when CGO cross-compilation is properly configured

archives:
  - format: binary
    name_template: "{{ .Binary }}_{{ .Os }}_{{ .Arch }}"
    # This creates individual binary files without version in name

snapshot:
  name_template: "v{{ .Version }}-snapshot"

changelog:
  use: git

release:
  github:
