sudo setcap 'cap_net_bind_service=+ep' /usr/local/bin/sp-caddy-manager

chmod 755 /etc/caddy /etc/caddy/conf.d
chmod 644 /etc/caddy/conf.d/*.caddy
chown -R caddy:caddy /etc/caddy/conf.d



/etc/caddy/Caddyfile
import /etc/caddy/conf.d/*.caddy

ensure at least one .caddy file exists (even an empty one)


caddy validate --config /etc/caddy/Caddyfile

caddy reload or service caddy restart ...



:80 {
    redir https://siliconpin.com/{uri} 301
}
:443 {
    reverse_proxy localhost
}

# Refer to the Caddy docs for more information:
# https://caddyserver.com/docs/caddyfile

import /etc/caddy/conf.d/*.caddy
